server:
  port: 8086

spring:
  application:
    name: stream-lambda-practice
  banner:
    charset: UTF-8

logging:
  level:
    com.example.sl: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level - %msg%n"
