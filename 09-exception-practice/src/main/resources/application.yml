# 应用配置
server:
  # 后端端口：8089（前序模块已用到 8088，本专题顺延）
  port: 8089

spring:
  application:
    name: exception-practice
  # 强制 UTF-8，避免 Windows 控制台中文乱码
  banner:
    charset: UTF-8

logging:
  level:
    # 教学项目：把自己包的日志开到 INFO，方便观察异常行为
    com.example.exception: INFO
